STEP 18C - CANONICAL CROSS-MARKET CONTEXT + CONTROLLED MODEL RESEARCH

Run every command in Command Prompt from:
C:\Users\sy771\Downloads\stock-intelligence

1. Back up the files that this step replaces:

tar -a -c -f pre-step18c-backup.zip package.json .env.example services\backend\src\nifty_terminal\research\step18b.py

2. Extract the supplied ZIP into the repository root:

tar -xf "%USERPROFILE%\Downloads\step18c-cross-market-context.zip" -C .

3. Open your private .env file:

notepad .env

Add these lines if they are not already present. Do not paste credentials here or send .env to anyone:

ANGELONE_BANKNIFTY_HISTORICAL_TOKEN=99926009
ANGELONE_INDIA_VIX_HISTORICAL_TOKEN=99926017

Keep this safety switch unchanged:

LIVE_SIGNAL_KILL_SWITCH=true

4. Run the structural and backend checks:

npm.cmd run check:step18c
npm.cmd run test:backend:windows

5. Acquire point-in-time Bank Nifty and India VIX history:

npm.cmd run acquire:context:angelone:windows

The command verifies both configured tokens against Angel One's current official instrument master before downloading. It prints and stores the exact context bundle path and SHA-256. If the provider master or data is unavailable, the command stops safely.

6. Run the controlled research comparison:

npm.cmd run research:cross-market:windows

The research command automatically uses the newest immutable context bundle. Runtime may be 40-120 minutes on a local CPU because it runs the NIFTY-only control and context-aware model on identical samples.

7. Send the final JSON report named at report_path. It will be under:

artifacts\research\cross-market-v1\

Do not send .env, the TOTP secret, PIN, API key, client code, or provider session tokens.

PASS/FAIL MEANING

- A completed run is not a released model.
- Both LONG and SHORT must improve versus the NIFTY-only control.
- Each direction must have a positive lower 95% confidence bound for Brier skill.
- The simulated-live policy must have enough BUY and SELL support and a positive lower 95% expectancy bound.
- Forward confirmation is still mandatory after all historical gates pass.
- No threshold is weakened to force approval.
- Official signals, exact dashboard probabilities, and automatic trading remain disabled.
