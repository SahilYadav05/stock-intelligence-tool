STEP 18E - LEAKAGE-SAFE RANKED UTILITY COHORT RESEARCH

IMPORTANT
- Run these commands from Windows Command Prompt in the repository root.
- Keep LIVE_SIGNAL_KILL_SWITCH=true.
- This step creates research evidence only. It cannot enable official signals or trading.
- Do not delete the Step 18D report. Step 18E is a methodological correction, not a rewrite.

1. Back up the files that this package may replace:

tar -a -c -f pre-step18e-backup.zip package.json contracts scripts docs services\backend\src\nifty_terminal services\backend\tests

2. Extract the supplied package into the repository root:

tar -xf "%USERPROFILE%\Downloads\step18e-ranked-utility.zip" -C .

3. Verify the package structure:

npm.cmd run check:step18e

4. Run the focused backend tests:

set PYTHONPATH=services/backend/src&& .venv\Scripts\python.exe -m unittest discover -s services/backend/tests -p test_ranked_utility_research.py

5. Run the complete project validation suite:

npm.cmd run test:windows

6. Run the real-data research. This may take 30-90 minutes on a local CPU:

npm.cmd run research:ranked-utility:windows

7. Send the complete terminal JSON output. The immutable report will also be under:

artifacts\research\ranked-utility-v1\

EXPECTED SAFETY STATE
- model_artifact_created: false
- approved_for_live_inference: false
- precise_probability_display_allowed: false
- official_signal_available: false
- automatic_trading_enabled: false

HOW TO READ THE RESULT
- A selected NIFTY_ONLY architecture is valid: it means context did not help that direction.
- passing_candidate_count = 0 means WAIT remains correct.
- The final historical replay is diagnostic only because this period has already been examined.
- Never weaken thresholds after seeing the result merely to make a gate pass.
